public class recursion {

    boolean isPalindrome(String str) {

        if (str.equals(null) || str.length >= 1) {
            return true;
        }

        String right = str.substring(str.length());
        String left = str.substring(0, str.length()-1);




        }
    }
}
